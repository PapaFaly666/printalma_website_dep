# Flux de Traitement : Customisations dans les Commandes

## 📊 Vue d'ensemble du flux

```
┌─────────────────────┐
│   FRONTEND          │
│  (React/TypeScript) │
└──────────┬──────────┘
           │
           │ 1. Client personnalise le produit
           │    (CustomerProductCustomizationPageV3)
           ▼
┌─────────────────────┐
│  Customization      │
│  Service            │
├─────────────────────┤
│ saveCustomization() │◄─── Pour chaque vue personnalisée
└──────────┬──────────┘
           │
           │ 2. Sauvegarde en BDD
           ▼
┌─────────────────────┐
│ Table:              │
│ customizations      │
├─────────────────────┤
│ id: 456             │
│ view_id: 5          │
│ design_elements: [] │
│ status: 'DRAFT'     │
│ order_id: NULL      │◄─── Pas encore lié à une commande
└─────────────────────┘

           │
           │ 3. Ajout au panier (CartContext)
           ▼
┌─────────────────────┐
│  localStorage       │
│  'cart'             │
├─────────────────────┤
│ [{                  │
│   customizationIds: │
│   {"1-5": 456,      │
│    "1-6": 457},     │
│   designElementsByView│
│   {...}             │
│ }]                  │
└──────────┬──────────┘
           │
           │ 4. Passage commande (OrderFormPage)
           ▼
┌─────────────────────┐
│  BACKEND API        │
│  POST /orders       │
└──────────┬──────────┘
           │
           │ 5. Traitement backend
           ▼

╔═══════════════════════════════════════════════════╗
║         TRAITEMENT BACKEND (DÉTAILLÉ)             ║
╚═══════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────┐
│ ÉTAPE 1 : Réception des données                │
├─────────────────────────────────────────────────┤
│ • Endpoint: POST /orders ou /orders/guest       │
│ • Body contient orderItems avec:               │
│   - customizationId                             │
│   - customizationIds                            │
│   - designElementsByView                        │
└────────────────┬────────────────────────────────┘
                 │
                 │ Validation
                 ▼
┌─────────────────────────────────────────────────┐
│ ÉTAPE 2 : Validation des données               │
├─────────────────────────────────────────────────┤
│ ✓ Vérifier productId existe                    │
│ ✓ Valider customizationIds (format)            │
│ ✓ Valider designElementsByView (structure)     │
│ ✓ Vérifier propriété des customizations        │
│                                                 │
│ ❌ Si invalide → Retourner erreur 400          │
└────────────────┬────────────────────────────────┘
                 │
                 │ Si valide
                 ▼
┌─────────────────────────────────────────────────┐
│ ÉTAPE 3 : Transaction START                    │
├─────────────────────────────────────────────────┤
│ BEGIN TRANSACTION;                              │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│ ÉTAPE 4 : Créer la commande                    │
├─────────────────────────────────────────────────┤
│ INSERT INTO orders (...) VALUES (...);          │
│                                                 │
│ Retourne: order_id = 789                       │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│ ÉTAPE 5 : Créer les order_items                │
├─────────────────────────────────────────────────┤
│ Pour chaque orderItem:                          │
│                                                 │
│ INSERT INTO order_items (                       │
│   order_id,                                     │
│   product_id,                                   │
│   quantity,                                     │
│   unit_price,                                   │
│   size,                                         │
│   color,                                        │
│   customization_id,                             │
│   customization_ids,          ◄─── IMPORTANT   │
│   design_elements_by_view     ◄─── IMPORTANT   │
│ ) VALUES (                                      │
│   789,                                          │
│   45,                                           │
│   1,                                            │
│   15000,                                        │
│   'M',                                          │
│   'Blanc',                                      │
│   456,                                          │
│   '{"1-5": 456, "1-6": 457}'::jsonb,           │
│   '{"1-5": [...], "1-6": [...]}'::jsonb        │
│ );                                              │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│ ÉTAPE 6 : Lier les customizations              │
├─────────────────────────────────────────────────┤
│ UPDATE customizations                           │
│ SET                                             │
│   order_id = 789,            ◄─── Lien commande│
│   status = 'ORDERED',        ◄─── Changement   │
│   updated_at = NOW()                            │
│ WHERE id IN (456, 457);      ◄─── Tous les IDs │
│                                                 │
│ Note: Extraire les IDs depuis customizationIds │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│ ÉTAPE 7 : Initier le paiement (si nécessaire)  │
├─────────────────────────────────────────────────┤
│ Si paymentMethod = 'PAYDUNYA':                  │
│   • Appeler API PayDunya                        │
│   • Récupérer token et redirect_url            │
│                                                 │
│ Si paymentMethod = 'CASH_ON_DELIVERY':          │
│   • Rien à faire                                │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│ ÉTAPE 8 : Transaction COMMIT                   │
├─────────────────────────────────────────────────┤
│ COMMIT;                                         │
│                                                 │
│ ❌ Si erreur à une étape → ROLLBACK            │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────┐
│ ÉTAPE 9 : Retourner la réponse                 │
├─────────────────────────────────────────────────┤
│ {                                               │
│   "success": true,                              │
│   "data": {                                     │
│     "id": 789,                                  │
│     "orderNumber": "ORD-2025-ABC123",          │
│     "payment": { ... }                          │
│   }                                             │
│ }                                               │
└─────────────────────────────────────────────────┘
```

---

## 🔄 État des données à chaque étape

### Avant la commande

**Table: customizations**
```
┌─────┬────────────┬──────────┬─────────┬──────────┐
│ id  │ product_id │ view_id  │ status  │ order_id │
├─────┼────────────┼──────────┼─────────┼──────────┤
│ 456 │ 45         │ 5        │ DRAFT   │ NULL     │ ◄─── Pas encore commandé
│ 457 │ 45         │ 6        │ DRAFT   │ NULL     │ ◄─── Pas encore commandé
└─────┴────────────┴──────────┴─────────┴──────────┘
```

### Après la commande

**Table: orders**
```
┌─────┬────────────────┬────────┬──────────────┐
│ id  │ order_number   │ status │ total_amount │
├─────┼────────────────┼────────┼──────────────┤
│ 789 │ ORD-2025-ABC1  │ PENDING│ 15000        │ ◄─── Nouvelle commande
└─────┴────────────────┴────────┴──────────────┘
```

**Table: order_items**
```
┌─────┬──────────┬────────────┬──────────────────┬────────────────────────────┐
│ id  │ order_id │ product_id │ customization_ids│ design_elements_by_view    │
├─────┼──────────┼────────────┼──────────────────┼────────────────────────────┤
│ 1234│ 789      │ 45         │ {"1-5": 456,     │ {"1-5": [...],             │
│     │          │            │  "1-6": 457}     │  "1-6": [...]}             │
└─────┴──────────┴────────────┴──────────────────┴────────────────────────────┘
                                       │
                                       │ Référence les IDs
                                       ▼
```

**Table: customizations**
```
┌─────┬────────────┬──────────┬─────────┬──────────┐
│ id  │ product_id │ view_id  │ status  │ order_id │
├─────┼────────────┼──────────┼─────────┼──────────┤
│ 456 │ 45         │ 5        │ ORDERED │ 789      │ ◄─── Maintenant lié
│ 457 │ 45         │ 6        │ ORDERED │ 789      │ ◄─── Maintenant lié
└─────┴────────────┴──────────┴─────────┴──────────┘
```

---

## 🔍 Cas d'usage détaillés

### Cas 1 : Commande simple (1 vue)

```
Client personnalise uniquement la vue "Devant"
                │
                ▼
┌───────────────────────────────┐
│ customizations créés:         │
│ • id: 100 (vue Devant)        │
└───────────────────────────────┘
                │
                │ Ajout au panier
                ▼
┌───────────────────────────────┐
│ Cart item:                    │
│ customizationIds: {"1-5": 100}│
└───────────────────────────────┘
                │
                │ Commande
                ▼
┌───────────────────────────────┐
│ order_items:                  │
│ customization_ids: {"1-5":100}│
└───────────────────────────────┘
                │
                │ UPDATE
                ▼
┌───────────────────────────────┐
│ customizations #100:          │
│ order_id = 789                │
│ status = 'ORDERED'            │
└───────────────────────────────┘
```

### Cas 2 : Commande complexe (3 vues)

```
Client personnalise:
• Vue Devant
• Vue Arrière
• Vue Manche droite
                │
                ▼
┌───────────────────────────────┐
│ customizations créés:         │
│ • id: 200 (Devant)            │
│ • id: 201 (Arrière)           │
│ • id: 202 (Manche)            │
└───────────────────────────────┘
                │
                │ Ajout au panier
                ▼
┌───────────────────────────────┐
│ Cart item:                    │
│ customizationIds: {           │
│   "1-5": 200,                 │
│   "1-6": 201,                 │
│   "1-7": 202                  │
│ }                             │
└───────────────────────────────┘
                │
                │ Commande
                ▼
┌───────────────────────────────┐
│ order_items:                  │
│ customization_ids: {          │
│   "1-5": 200,                 │
│   "1-6": 201,                 │
│   "1-7": 202                  │
│ }                             │
└───────────────────────────────┘
                │
                │ UPDATE (3 enregistrements)
                ▼
┌───────────────────────────────┐
│ customizations:               │
│ #200: order_id=789, ORDERED   │
│ #201: order_id=789, ORDERED   │
│ #202: order_id=789, ORDERED   │
└───────────────────────────────┘
```

---

## ⚡ Points critiques de performance

### Optimisations recommandées

1. **Index JSONB**
```sql
CREATE INDEX idx_order_items_customization_ids
ON order_items USING gin(customization_ids);

CREATE INDEX idx_order_items_design_elements
ON order_items USING gin(design_elements_by_view);
```

2. **Requête efficace pour mise à jour**
```javascript
// ✅ BIEN : Une seule requête pour tous les IDs
const ids = Object.values(item.customizationIds);
await Customization.update(
  { orderId, status: 'ORDERED' },
  { where: { id: { [Op.in]: ids } } }
);

// ❌ ÉVITER : Boucle avec requêtes individuelles
for (const id of ids) {
  await Customization.update(
    { orderId, status: 'ORDERED' },
    { where: { id } }
  );
}
```

3. **Transaction unique**
```javascript
// ✅ BIEN : Tout dans une transaction
await sequelize.transaction(async (t) => {
  const order = await Order.create({...}, { transaction: t });
  await OrderItem.create({...}, { transaction: t });
  await Customization.update({...}, { transaction: t });
});

// ❌ ÉVITER : Requêtes séparées sans transaction
const order = await Order.create({...});
await OrderItem.create({...});
await Customization.update({...});
```

---

## 🐛 Debugging et logs

### Logs recommandés

```javascript
// À chaque étape importante
console.log('📦 [OrderService] Création commande', {
  userId: user?.id,
  itemCount: orderItems.length,
  hasCustomizations: orderItems.some(i => i.customizationIds)
});

console.log('🎨 [OrderService] Customisations détectées', {
  customizationIds: item.customizationIds,
  viewsCount: Object.keys(item.customizationIds || {}).length
});

console.log('✅ [OrderService] Customisations liées', {
  orderId,
  customizationIds: Object.values(item.customizationIds)
});
```

### Vérifications SQL

```sql
-- Vérifier la cohérence après création
SELECT
  oi.id,
  oi.customization_ids,
  array_agg(c.id) AS linked_customizations,
  array_agg(c.status) AS customization_statuses
FROM order_items oi
LEFT JOIN customizations c ON c.id = ANY(
  SELECT value::integer
  FROM jsonb_each_text(oi.customization_ids)
)
WHERE oi.order_id = 789
GROUP BY oi.id;
```

---

**Version** : 1.0
**Date** : 2025-01-17
**Mis à jour** : Lors des changements du flux
