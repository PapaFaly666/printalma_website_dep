# Configuration pour ngrok - PayTech exige des URLs HTTPS
# Copier ce fichier en .env.local et remplacer avec votre URL ngrok

# URL ngrok pour le backend (port 3004)
# Lancer ngrok avec: ngrok http 3004
# Remplacer https://abc123.ngrok.io par votre URL ngrok réelle
VITE_NGROK_URL=https://your-ngrok-url.ngrok.io

# URL de l'API backend
VITE_API_URL=http://localhost:3004

# Configuration PayTech (optionnel - les valeurs par défaut sont utilisées)
# VITE_PAYTECH_ENV=test